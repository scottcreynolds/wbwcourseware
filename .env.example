# Vercel/browser build: application label shown in the UI.
VITE_APP_NAME=Writers Be Writing
# Vercel/browser build: public values from `supabase start` locally or the project settings in production.
VITE_SUPABASE_URL=http://127.0.0.1:55321
VITE_SUPABASE_ANON_KEY=replace-with-local-anon-key-from-supabase-start
